<Window x:Class="SystemMonitor.SolutionGuideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hướng dẫn chi tiết giải pháp" Height="750" Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="600" MinWidth="700"
        Background="#F5F7FA">

    <Window.Resources>
        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D9E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.15" 
                                    BlurRadius="12" ShadowDepth="3" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1E88E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1E88E5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                                <Setter Property="BorderBrush" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                                <Setter Property="BorderBrush" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close Button Style -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#EF5350"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#EF5350"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E53935"/>
                                <Setter Property="BorderBrush" Value="#E53935"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C62828"/>
                                <Setter Property="BorderBrush" Value="#C62828"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Expander Style -->
        <Style x:Key="ExpanderStyle" TargetType="Expander">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#D1D9E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <DockPanel>
                                <ToggleButton x:Name="HeaderSite" 
                                             DockPanel.Dock="Top" 
                                             Content="{TemplateBinding Header}"
                                             Background="#E8ECEF"
                                             BorderThickness="0"
                                             Padding="15,10"
                                             FontWeight="SemiBold"
                                             Foreground="#37474F"
                                             IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <ContentPresenter x:Name="ContentSite" 
                                                 Visibility="Collapsed"
                                                 Content="{TemplateBinding Content}"
                                                 Margin="10"/>
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource CardStyle}" 
                Background="#F8FAFC">
            <StackPanel Margin="20">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Border Background="#BBDEFB" Padding="10" Margin="0,0,15,0" CornerRadius="6">
                        <TextBlock Text="📖" FontSize="22"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="SolutionTitle" 
                                   Text="Hướng dẫn chi tiết giải pháp" 
                                   FontSize="24" FontWeight="Bold" 
                                   Foreground="#0D47A1"/>
                        <TextBlock x:Name="SolutionType" 
                                   Text="Khắc phục sự cố hệ thống" 
                                   FontSize="14" Foreground="#546E7A"
                                   Margin="0,5,0,0"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Guide Content -->
        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CardStyle}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel>
                    <!-- Description Section -->
                    <Expander Style="{StaticResource ExpanderStyle}" Header="💡 Mô tả giải pháp">
                        <TextBlock x:Name="SolutionDescription" 
                                   Text="Mô tả chi tiết về giải pháp sẽ được hiển thị ở đây"
                                   TextWrapping="Wrap" FontSize="14" 
                                   Foreground="#37474F" LineHeight="22"
                                   Margin="0,10,0,0"/>
                    </Expander>

                    <!-- Steps Section -->
                    <Expander Style="{StaticResource ExpanderStyle}" Header="🔧 Các bước thực hiện" IsExpanded="True">
                        <TextBlock x:Name="GuideContent" 
                                   TextWrapping="Wrap" 
                                   FontFamily="Segoe UI"
                                   FontSize="14"
                                   LineHeight="24"
                                   Foreground="#263238"
                                   Margin="0,10,0,0"
                                   Text="Chi tiết các bước thực hiện sẽ được hiển thị ở đây..."/>
                    </Expander>

                    <!-- Warning Section -->
                    <Expander Style="{StaticResource ExpanderStyle}" Header="⚠️ Lưu ý quan trọng">
                        <TextBlock x:Name="WarningContent" 
                                   Text="• Thực hiện theo đúng thứ tự các bước được hướng dẫn
• Sao lưu dữ liệu quan trọng trước khi thực hiện
• Khởi động lại hệ thống nếu cần thiết
• Liên hệ hỗ trợ kỹ thuật nếu gặp khó khăn" 
                                   TextWrapping="Wrap" FontSize="13" 
                                   Foreground="#BF360C" LineHeight="20"
                                   Margin="0,10,0,0"/>
                    </Expander>

                    <!-- Additional Resources -->
                    <Expander Style="{StaticResource ExpanderStyle}" Header="🔗 Tài nguyên bổ sung">
                        <TextBlock x:Name="ResourcesContent" 
                                   Text="• Tài liệu hướng dẫn chi tiết trên website chính thức
• Video hướng dẫn trực quan
• Cộng đồng hỗ trợ và FAQ
• Liên hệ bộ phận kỹ thuật: support@example.com" 
                                   TextWrapping="Wrap" FontSize="13" 
                                   Foreground="#1B5E20" LineHeight="20"
                                   Margin="0,10,0,0"/>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Quick Tips Sidebar -->
        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardStyle}">
            <StackPanel Margin="15">
                <TextBlock Text="🚀 Mẹo nhanh" FontSize="16" FontWeight="Bold" 
                           Foreground="#1565C0" Margin="0,0,0,15"/>
                <TextBlock x:Name="QuickTipsContent" 
                           Text="• Theo dõi hiệu quả sau mỗi bước
• Lưu cài đặt gốc trước khi thay đổi
• Kiểm tra tài nguyên hệ thống thường xuyên
• Liên hệ hỗ trợ nếu gặp vấn đề" 
                           TextWrapping="Wrap" FontSize="13" 
                           Foreground="#455A64" LineHeight="20"/>
            </StackPanel>
        </Border>

        <!-- Action Buttons -->
        <StackPanel x:Name="ActionButtonsPanel" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="❌ Đóng" 
                    Style="{StaticResource CloseButtonStyle}"
                    MinWidth="100"
                    Click="Close_Click"
                    ToolTip="Đóng cửa sổ hướng dẫn"/>
        </StackPanel>
    </Grid>
</Window>