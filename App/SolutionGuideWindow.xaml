<Window x:Class="SystemMonitor.SolutionGuideWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hướng dẫn chi tiết giải pháp" Height="700" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="600" MinWidth="600">

    <Window.Resources>
        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E6ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.1" 
                                    BlurRadius="10" ShadowDepth="2" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#4CAF50"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3D8B40"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" 
                Background="#F8FAFC" Margin="0,0,0,20">
            <StackPanel Margin="25,20">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Border Background="#E3F2FD" Padding="12" Margin="0,0,15,0" CornerRadius="8">
                        <TextBlock Text="📖" FontSize="24"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="SolutionTitle" 
                                   Text="Hướng dẫn chi tiết giải pháp" 
                                   FontSize="22" FontWeight="Bold" 
                                   Foreground="#1565C0"/>
                        <TextBlock x:Name="SolutionType" 
                                   Text="Khắc phục sự cố hệ thống" 
                                   FontSize="14" Foreground="#64748B"
                                   Margin="0,5,0,0"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Guide Content -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="30">
                <StackPanel>
                    <!-- Description Section -->
                    <Border Background="#F0F8FF" BorderBrush="#2196F3" 
                            BorderThickness="1" Padding="20" Margin="0,0,0,25" CornerRadius="8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <TextBlock Text="💡" FontSize="18" Margin="0,0,10,0" 
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="Mô tả giải pháp" 
                                           FontSize="16" FontWeight="Bold" 
                                           Foreground="#1976D2" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock x:Name="SolutionDescription" 
                                       Text="Mô tả chi tiết về giải pháp sẽ được hiển thị ở đây"
                                       TextWrapping="Wrap" FontSize="14" 
                                       Foreground="#37474F" LineHeight="22"/>
                        </StackPanel>
                    </Border>

                    <!-- Steps Section -->
                    <StackPanel Margin="0,0,0,25">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="🔧" FontSize="18" Margin="0,0,10,0" 
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="Các bước thực hiện" 
                                       FontSize="16" FontWeight="Bold" 
                                       Foreground="#1565C0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <TextBlock x:Name="GuideContent" 
                                   TextWrapping="Wrap" 
                                   FontFamily="Segoe UI"
                                   FontSize="14"
                                   LineHeight="24"
                                   Foreground="#263238"
                                   Background="White"
                                   Padding="20"
                                   Text="Chi tiết các bước thực hiện sẽ được hiển thị ở đây..."/>
                    </StackPanel>

                    <!-- Warning Section -->
                    <Border Background="#FFF3E0" BorderBrush="#FF9800" 
                            BorderThickness="1" Padding="20" Margin="0,0,0,25" CornerRadius="8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="⚠️" FontSize="16" Margin="0,0,10,0" 
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="Lưu ý quan trọng" 
                                           FontSize="14" FontWeight="Bold" 
                                           Foreground="#E65100" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="• Thực hiện theo đúng thứ tự các bước được hướng dẫn&#x0a;• Sao lưu dữ liệu quan trọng trước khi thực hiện&#x0a;• Khởi động lại hệ thống nếu cần thiết&#x0a;• Liên hệ hỗ trợ kỹ thuật nếu gặp khó khăn" 
                                       TextWrapping="Wrap" FontSize="13" 
                                       Foreground="#BF360C" LineHeight="20"/>
                        </StackPanel>
                    </Border>

                    <!-- Additional Resources -->
                    <Border Background="#E8F5E8" BorderBrush="#4CAF50" 
                            BorderThickness="1" Padding="20" CornerRadius="8">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="🔗" FontSize="16" Margin="0,0,10,0" 
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="Tài nguyên bổ sung" 
                                           FontSize="14" FontWeight="Bold" 
                                           Foreground="#2E7D32" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="• Tài liệu hướng dẫn chi tiết trên website chính thức&#x0a;• Video hướng dẫn trực quan&#x0a;• Cộng đồng hỗ trợ và FAQ&#x0a;• Liên hệ bộ phận kỹ thuật: support@example.com" 
                                       TextWrapping="Wrap" FontSize="13" 
                                       Foreground="#1B5E20" LineHeight="20"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">

            <Button Content="❌ Đóng" 
                    Background="#6C757D" Foreground="White"
                    BorderBrush="#6C757D" BorderThickness="1"
                    Padding="20,12" FontWeight="SemiBold" FontSize="14"
                    MinWidth="100"
                    Click="Close_Click"
                    ToolTip="Đóng cửa sổ hướng dẫn"/>
        </StackPanel>
    </Grid>
</Window>